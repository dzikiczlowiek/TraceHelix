## Scope

<!-- What does this change do and why? Reference the bounded v0.1.0 scope and
docs/release-readiness-v0.1.0.md if relevant. State whether application
behavior changes. -->

## Tests and evidence

<!-- Which focused tests were added (RED first), and what command output proves
GREEN? Attach the relevant repository-guard, dotnet, web, or training output.
Do not paste raw trace data. -->

## Release-claim updates

<!-- Does this change version sources (VERSION, Directory.Build.props,
web/package.json, training/pyproject.toml), release-scope claims, README, or
docs/release-readiness-v0.1.0.md? If so, describe the update and confirm the
guards still pass. State "no change" if not. -->

## Privacy check

<!-- Confirm no traces, secrets, credentials, API keys, tokens, private keys,
paths, prompts, or private data are added to the diff (including fixtures,
snapshots, or log excerpts). Raw traces stay local. -->

## Exact-snapshot note

<!-- Record the python scripts/source_fingerprint.py value before the change,
after the change, and after staging. The reviewed fingerprint must match before
and after `git add --all`. Green tests from a different fingerprint are not
substitute evidence. -->
